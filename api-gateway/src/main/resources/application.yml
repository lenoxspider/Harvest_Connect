server:
  port: 8080

spring:
  application:
    name: harvestconnect-api-gateway
  cloud:
    gateway:
      # Enable CORS for all origins and methods
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders: "*"
      
      # Define routing rules
      routes:
        - id: auth-service
          uri: http://${AUTH_SERVICE_URL:localhost:8081}
          predicates:
            - Path=/api/auth/**

        - id: produce-service
          uri: http://${PRODUCE_SERVICE_URL:localhost:8082}
          predicates:
            - Path=/api/produce/**

        - id: transport-service
          uri: http://${TRANSPORT_SERVICE_URL:localhost:8083}
          predicates:
            - Path=/api/transport/**

        - id: storage-service
          uri: http://${STORAGE_SERVICE_URL:localhost:8084}
          predicates:
            - Path=/api/storage/**

        - id: payments-service
          uri: http://${PAYMENT_SERVICE_URL:localhost:8085}
          predicates:
            - Path=/api/payments/**

        - id: notifications-service
          uri: http://${NOTIFICATION_SERVICE_URL:localhost:8086}
          predicates:
            - Path=/api/notifications/**

        - id: kyc-service
          uri: http://${KYC_SERVICE_URL:localhost:8087}
          predicates:
            - Path=/api/kyc/**
